            // now here I'll figure out which half is sorted            // check if the left half is sorted or the right half            // left sorted            if (nums[start] <= nums[mid]) {                if (nums[start] <= target && target <= nums[mid]) {                    end = mid - 1;                } else {                    start = mid + 1;                }            // right sorted            } else {            // if the array is rotated, at least one half is always sorted.                return mid;            if(nums[mid] == target)            int mid = start + (end - start) / 2;        while (start <= end) {        int end = n - 1;        int start = 0;        int n = nums.length;    public int search(int[] nums, int target) {class Solution {