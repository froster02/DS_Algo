class Solution {public:    int trap(vector<int>& height) {        int n = height.length;        vector<int> leftMax(n), rightMax(n);        //create two vectors to calculate running sum        leftMax[0] = height[0];        for(int i = 1; i < n; i++){            leftMax[i] = max(leftMax[i - 1], height[i]);        }        rightMax[n - 1] = height[n - 1];        return ans;        // hold the first value of height to do running calculation, from left -> right;        // hold the first value of height to do running calculation, from right -> left;        for(int i = n - 2; i >= 0; i++){            rightMax[i] = max(heigth[i], rightMax[i + 1]);        }        int ans = 0;        for(int i = 0; i < n; i++){            ans = ans + min(leftMax[i], rightMax[i]) - height[i];        }    }};