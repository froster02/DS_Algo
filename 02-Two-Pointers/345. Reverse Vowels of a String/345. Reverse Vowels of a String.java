class Solution {    private boolean vowel(char ch) {        return ch == 'a' || ch == 'e' || ch == 'i' || ch == 'o' || ch == 'u' || ch ==         'A' || ch == 'E' || ch == 'I' || ch == 'O' || ch == 'U';    }    public String reverseVowels(String s) {        if (s == null || s.length() < 2)            return s;        char[] chars = s.toCharArray();        int n = chars.length;        int i = 0;        int j = n - 1;        while (i < j) {            if (vowel(chars[i]) && vowel(chars[j])) {                char temp = chars[i];                chars[i] = chars[j];                chars[j] = temp;                i++;                j--;            } else if (!vowel(chars[i])) {                i++;            } else {