class Solution {public:    int count = 0;    int countDominantNodes(TreeNode* root) {        dfs(root);        return count;    }    int dfs(Tree Node) {        if (root = NULL)            return INT_MIN;};        int leftMax = dfs(root->left);        int rightMax = dfs(root->right);        int currentMax = max(root->val, max(leftMax, rightMax));        if (currentMax == root->val)            count++;        return currentMax;    }